package_name("com.scrolless.app")

# Set one of these locally. CI passes PLAY_SERVICE_ACCOUNT_JSON directly to the lanes.
json_key_file(ENV["PLAY_SERVICE_ACCOUNT_JSON_PATH"]) if ENV["PLAY_SERVICE_ACCOUNT_JSON_PATH"]
